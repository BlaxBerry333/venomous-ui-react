import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./ThemePreset.stories";

<Meta of={Stories} />

# ThemeProvider

`<ThemeProvider>` is an optional utility component, but strongly recommended to use to achieve a consistent theme style.

- Initialize global `ThemeColor` and `ThemeMode` during App startup.
- Inject dynamic CSS styles into HTML ( such as scrollbars、text selection highlights )

<Canvas of={Stories.Default} sourceState="none" />

<Controls />

```tsx
import { ThemePreset } from "venomous-ui-react/components";

function App() {
  return (
    <>
      <ThemePreset defaultThemeMode={...} defaultThemeColor={...} />

      {/* App Content */}
    </>
  );
}
```
