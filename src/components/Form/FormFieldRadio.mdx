import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldRadio.stories";

<Meta of={Stories} />

# FormField.Radio

`<FormField.Text>` will be rendered as a combination of a group of HTML tag `<input type="radio"/>` + `<label/>`.

<Canvas of={Stories.Default} />

<Controls />
