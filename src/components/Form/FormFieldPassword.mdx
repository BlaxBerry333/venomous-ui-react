import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldPassword.stories";

<Meta of={Stories} />

# FormField.Password

`<FormField.Password>` will be rendered as a combination of HTML tag `<input type="password"/>` + `<label/>`.

<Canvas of={Stories.Default} />

<Controls />
