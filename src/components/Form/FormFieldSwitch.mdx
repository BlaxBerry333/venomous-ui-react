import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldSwitch.stories";

<Meta of={Stories} />

# FormField.Switch

`<FormField.Switch>` is functionally equivalent to `<FormField.Checkbox>`

<Canvas of={Stories.Default} />

<Controls />
