import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldCheckbox.stories";

<Meta of={Stories} />

# FormField.Checkbox

`<FormField.Checkbox>` will be rendered as a combination of HTML tag `<input type="checkbox"/>` + `<label/>`.

<Canvas of={Stories.Default} />

<Controls />
