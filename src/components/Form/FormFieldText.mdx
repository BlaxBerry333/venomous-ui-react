import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldText.stories";

<Meta of={Stories} />

# FormField.Text

`<FormField.Text>` will be rendered as a combination of HTML tag `<input type="text"/>` + `<label/>`.

<Canvas of={Stories.Default} />

<Controls />
