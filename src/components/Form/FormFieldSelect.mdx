import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./FormFieldSelect.stories";

<Meta of={Stories} />

# FormField.Select

`<FormField.Select>` will be rendered as a combination of HTML tag `<select/>` + `<label/>`.

<Canvas of={Stories.Default} />

<Controls />
