import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./TypographyTitle.stories";

<Meta of={Stories} />

# Typography.Title

`<Typography.Title>` will be rendered as HTML tag `<h1 ~ h6>`.

<Canvas of={Stories.Default} />

<Controls />
