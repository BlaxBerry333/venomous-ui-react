import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./TypographyCode.stories";

<Meta of={Stories} />

# Typography.Code

`<Typography.Code>` will be rendered as HTML tag `<code>`.

<Canvas of={Stories.Default} />

<Controls />
