import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./MenuItem.stories";

<Meta of={Stories} />

# Menu.Item

`<Menu.Item>` will be rendered as HTML tag `<li>`、`<dt>`、`<dd>`.

Strongly recommended for use with `<Menu.List>`.

<Canvas of={Stories.Default} />

<Controls />
