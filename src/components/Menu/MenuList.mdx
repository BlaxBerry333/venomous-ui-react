import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./MenuList.stories";

<Meta of={Stories} />

# Menu.List

`<Menu.List>` will be rendered as HTML tag `<ul>`、`<ol>`、`<dl>`.

Strongly recommended for use with `<Menu.Item>`.

<Canvas of={Stories.Default} />

<Controls />
