import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./Button.stories";

<Meta of={Stories} />

# Button

<Canvas of={Stories.Default} />

<Controls />

## TroubleShotting

<br />

#### `color` does not work when `variant="ghost"`

Yes, that's right. As the name, ghost button not carry any color.
