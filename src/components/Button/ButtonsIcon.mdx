import { Meta, Canvas, Controls } from "@storybook/blocks";
import * as Stories from "./ButtonsIcon.stories";

<Meta of={Stories} />

# Buttons.Icon

<Canvas of={Stories.Default} />

<Controls />

## TroubleShotting

<br />

#### `color` does not work when `variant="ghost"`

Yes, that's right. As the name, ghost button do not carry any color.
