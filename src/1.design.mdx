import { Meta } from "@storybook/blocks";

import { Card, Space, Typography } from "./components";
import {
  Colors,
  BackgroundColors,
  ThemeColor,
  BorderColors,
  ShadowColors,
  TextColors,
  IconColors,
  ButtonColors,
} from "./utils";

<Meta title="Design" />

# Design

<br />

## ThemeColor

<Space.Grid columns={{ xs: 1, sm: 2, md: 3, lg: 3, xl: 3, xxl: 3 }}>
  {Object.entries(ThemeColor).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" color="white" text={`ThemeColor.${name}`} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { ThemeColor } from "venomous-ui-react/utils";

enum ThemeColor {
  ScarletViper = "#C62828", // 猩红蝮蛇
  JadeAnaconda = "#26A69A", // 翡翠森蚺
  AmethystRattlesnake = "#7E57C2", // 紫晶响尾蛇
  AmberCobra = "#FFA000", // 琥珀眼镜蛇
  ObsidianBothrops = "#546E7A", // 石墨矛头蝰
}
```

## Colors

<Space.Grid columns={{ xs: 2, sm: 3, md: 4, lg: 4, xl: 4, xxl: 4 }}>
  {Object.entries(Colors).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" color="grey" text={`Colors.${name}`} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { Colors } from "venomous-ui-react/utils";

const Colors = {
  disabled: "#e4edf4",
  success: "#4caf50",
  error: "#f44336",
  warning: "#ff9800",
  info: "#2196f3",
} as const;
```

## BackgroundColors

<Space.Grid columns={{ xs: 1, sm: 2, md: 3, lg: 3, xl: 3, xxl: 3 }}>
  {Object.entries(BackgroundColors).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" text={`BackgroundColors.${name}`} style={{ color: "#7a818c" }} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { BackgroundColors } from "venomous-ui-react/utils";

const BackgroundColors = {
  darkMode: "#181818",
  lightMode: "#ffffff",
} as const;
```

## BorderColors

<Space.Grid
  columns={{ xs: 1, sm: 2, md: 3, lg: 3, xl: 3, xxl: 3 }}
  style={{ backgroundColor: "#7a818c", padding: "32px 16px" }}
>
  {Object.entries(BorderColors).map(([name, value]) => (
    <Card key={name} isOutline style={{ border: `2px solid ${value}`, backgroundColor: "#7a818c" }}>
      <Typography.Text as="strong" color="white" text={`BorderColors.${name}`} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { BorderColors } from "venomous-ui-react/utils";

const BorderColors = {
  darkMode: "rgba(255, 255, 255, 0.1)",
  lightMode: "rgba(0, 0, 0, 0.1)",
} as const;
```

## ShadowColors

<Space.Grid
  columns={{ xs: 1, sm: 2, md: 3, lg: 3, xl: 3, xxl: 3 }}
  style={{ backgroundColor: "#7a818c", padding: "32px 16px" }}
>
  {Object.entries(ShadowColors).map(([name, value]) => (
    <Card key={name} isOutline={false} style={{ boxShadow: value, backgroundColor: "#7a818c" }}>
      <Typography.Text as="strong" color="white" text={`ShadowColors.${name}`} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { ShadowColors } from "venomous-ui-react/utils";

const ShadowColors = {
  darkMode: `
    rgba(255, 255, 255, 0.2) 0px 3px 5px -1px, 
    rgba(255, 255, 255, 0.14) 0px 6px 10px 0px, 
    rgba(255, 255, 255, 0.12) 0px 1px 8px 0px
    `,
  lightMode: `
    rgba(0, 0, 0, 0.2) 0px 3px 5px -1px, 
    rgba(0, 0, 0, 0.14) 0px 6px 10px 0px, 
    rgba(0, 0, 0, 0.12) 0px 1px 8px 0px
    `,
} as const;
```

## TextColors

<Space.Grid columns={{ xs: 1, sm: 2, md: 3, lg: 3, xl: 3, xxl: 3 }}>
  {Object.entries(TextColors).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" text={`TextColors.${name}`} style={{ color: "#7a818c" }} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { TextColors } from "venomous-ui-react/utils";

const TextColors = {
  darkMode: "#cccccc",
  lightMode: "#212121",
  grey: "#666666",
  white: "#ffffff",
  link: "#1976d2",
  disabled: Colors.disabled,
  success: Colors.success,
  error: Colors.error,
  warning: Colors.warning,
} as const;
```

## IconColors

<Space.Grid columns={{ xs: 1, sm: 2, md: 3, lg: 4, xl: 4, xxl: 4 }}>
  {Object.entries(IconColors).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" text={`IconColors.${name}`} style={{ color: "#7a818c" }} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { IconColors } from "venomous-ui-react/utils";

const IconColors = {
  auto: "auto", // color: "inherit"
  white: "#ffffff",
  success: Colors.success,
  error: Colors.error,
  warning: Colors.warning,
  info: Colors.info,
} as const;
```

## ButtonColors

<Space.Grid columns={{ xs: 1, sm: 2, md: 3, lg: 4, xl: 4, xxl: 4 }}>
  {Object.entries(ButtonColors).map(([name, value]) => (
    <Card key={name} isOutline style={{ backgroundColor: value }}>
      <Typography.Text as="strong" text={`ButtonColors.${name}`} style={{ color: "#7a818c" }} />
    </Card>
  ))}
</Space.Grid>

```ts
// import { ButtonColors } from "venomous-ui-react/utils";

const ButtonColors = {
  auto: "auto", // color: "inherit"
  disabled: Colors.disabled,
  error: Colors.error,
} as const;
```
