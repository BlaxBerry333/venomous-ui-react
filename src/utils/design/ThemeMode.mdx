import { Meta } from "@storybook/blocks";

<Meta title="utils/design/ThemeMode" />

# ThemeMode

The component library provides `light` and `dark` theme modes.

```tsx
import { THEME_MODES, type ThemeMode } from "venomous-ui-react/utils/design";
```
