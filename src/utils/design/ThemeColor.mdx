import { Meta } from "@storybook/blocks";

<Meta title="utils/design/ThemeColor" />

# ThemeColor

The component library provides a series of built-in default theme colors that can be used directly.

```tsx
import { THEME_COLORS, type ThemeColorName } from "venomous-ui-react/utils/design";
```
